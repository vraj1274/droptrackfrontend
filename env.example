###############################################
# DropVerify Frontend - Environment Variables #
###############################################

# API Base URL
# PRODUCTION: Must use HTTPS (e.g., https://api.droptrack.com.au)
# DEVELOPMENT: Can use HTTP for localhost (e.g., http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8000

# Optional legacy aliases (recognized by src/lib/api.ts)
# VITE_BACKEND_URL=http://localhost:8000
# VITE_API_URL=http://localhost:8000

# AWS Cognito Configuration
# IMPORTANT: The App Client MUST NOT have a client secret enabled
# (Public client for browser-based applications)
#
# PRODUCTION: Must use production Cognito User Pool (not test)
# Get values from: AWS Cognito Console
VITE_COGNITO_USER_POOL_ID=us-east-1_your_user_pool_id
VITE_COGNITO_CLIENT_ID=your_app_client_id
VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com

# Cognito OAuth Redirect URIs
# PRODUCTION: Must use HTTPS (e.g., https://droptrack.com.au/auth/callback)
# DEVELOPMENT: Can use HTTP for localhost
VITE_COGNITO_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_COGNITO_LOGOUT_URI=http://localhost:5173/

# Google Maps API Key
# Get from: https://console.cloud.google.com/google/maps-apis
# PRODUCTION: Must be configured with production domain restrictions
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Stripe Publishable Key
# Get from: https://dashboard.stripe.com/apikeys
# PRODUCTION: Must use live key (pk_live_...)
# DEVELOPMENT: Can use test key (pk_test_...)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key






